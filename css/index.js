/* 
    css specificity: inline > external inline > internal, 
    selectors: id > class id>element, union, direct child, descendent, sibling, pseudo selector
            color
            background
            display: inline, block, inline-block, grid(2 dimension), none(visibility:hidden), inline-flex,flex
            position:fixed relative absolute sticky static(default), absolute position will relocate the element related to its nearest parent element with non static position
            box-model
            box-sizing: border-box content-box
            inset
            width height
            padding margin padding-left padding-top..
            border outline
            font font-size font-color
            z-index
            flexbox: one dimensional layout system
            align-items:secondary axis
            justify-content:main axis
*/